schemaVersion: 2.2.2
metadata:
  name: shopping-cart-react-devspace
components:
  - name: tools
    container:
      image: quay.io/cgruver0/che/sshd-workspace-base:stable
      endpoints:
        - exposure: public
          name: nodejs
          protocol: https
          targetPort: 3000
      memoryLimit: '6Gi'
      memoryRequest: '1Gi'
      cpuLimit: '1'
      cpuRequest: '0.5'
      mountSources: true
      volumeMounts:
        - name: npm
          path: /home/user/.npm
  - name: npm
    volume:
      size: 1G    
commands:
  - id: 1-dependencies
    exec:
      label: "Download dependencies"
      component: tools
      workingDir: /projects/shopping-cart-react-devspace
      commandLine: "npm install"
      group:
        kind: build
  - id: 2-run
    exec:
      label: "Run the web app"
      component: tools
      workingDir: /projects/shopping-cart-react-devspace
      commandLine: "npm start"
      group:
        kind: run
  - id: 3-install-and-run
    exec:
      label: "Run the web app (and download dependencies)"
      component: tools
      workingDir: /projects/shopping-cart-react-devspace
      commandLine: "npm install; npm start"
      group:
        kind: run
  - id: 4-test
    exec:
      label: "Run the web app (and download dependencies)"
      component: tools
      workingDir: /projects/shopping-cart-react-devspace
      commandLine: "npm test"
      group:
        kind: test


